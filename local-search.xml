<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Docker基本使用</title>
    <link href="/2023/04/03/Docker%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/"/>
    <url>/2023/04/03/Docker%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/</url>
    
    <content type="html"><![CDATA[<h3 id="hint：docker镜像库中包含大量可用镜像，可以直接下载到本地使用，常用的镜像库比如https-hub-docker-com"><a href="#hint：docker镜像库中包含大量可用镜像，可以直接下载到本地使用，常用的镜像库比如https-hub-docker-com" class="headerlink" title="hint：docker镜像库中包含大量可用镜像，可以直接下载到本地使用，常用的镜像库比如https://hub.docker.com/"></a>hint：docker镜像库中包含大量可用镜像，可以直接下载到本地使用，常用的镜像库比如<a href="https://hub.docker.com/">https://hub.docker.com/</a></h3><h4 id="1-在https-cloud-docker-com，免费注册一个-Docker-账号。用以下命令登录docker-hub，输入创建账号的用户名、密码"><a href="#1-在https-cloud-docker-com，免费注册一个-Docker-账号。用以下命令登录docker-hub，输入创建账号的用户名、密码" class="headerlink" title="1.在https://cloud.docker.com，免费注册一个 Docker 账号。用以下命令登录docker hub，输入创建账号的用户名、密码"></a>1.在<a href="https://cloud.docker.com,免费注册一个/">https://cloud.docker.com，免费注册一个</a> Docker 账号。用以下命令登录<code>docker hub</code>，输入创建账号的用户名、密码</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">$ docker login </span><br><span class="line">Login with your Docker ID to push and pull images from Docker Hub. If you don<span class="string">&#x27;t have a Docker ID, head over to https://hub.docker.com to create one.</span></span><br><span class="line"><span class="string">Username: linlinshe</span></span><br><span class="line"><span class="string">Password: </span></span><br><span class="line"><span class="string">WARNING! Your password will be stored unencrypted in /root/.docker/config.json.</span></span><br><span class="line"><span class="string">Configure a credential helper to remove this warning. See</span></span><br><span class="line"><span class="string">https://docs.docker.com/engine/reference/commandline/login/#credentials-store</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">Login Succeeded</span></span><br></pre></td></tr></table></figure><h4 id="出现login-succeeded表示登陆成功"><a href="#出现login-succeeded表示登陆成功" class="headerlink" title="出现login succeeded表示登陆成功"></a>出现<code>login succeeded</code>表示登陆成功</h4><h4 id="2-将如下内容写入-etc-docker-daemon-json中（没有此文件则需要新建-ps-需保证docker已经运行过，否则会出现-etc-docker文件夹不存在）"><a href="#2-将如下内容写入-etc-docker-daemon-json中（没有此文件则需要新建-ps-需保证docker已经运行过，否则会出现-etc-docker文件夹不存在）" class="headerlink" title="2.将如下内容写入/etc/docker/daemon.json中（没有此文件则需要新建. ps: 需保证docker已经运行过，否则会出现/etc/docker文件夹不存在）"></a>2.将如下内容写入<code>/etc/docker/daemon.json</code>中（没有此文件则需要新建. ps: 需保证<code>docker</code>已经运行过，否则会出现<code>/etc/docker</code>文件夹不存在）</h4><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;registry-mirrors&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;https://uhub-edu.service.ucloud.cn&quot;</span><span class="punctuation">,</span><span class="string">&quot;https://uhub.service.ucloud.cn&quot;</span><span class="punctuation">,</span><span class="string">&quot;https://registry.docker-cn.com&quot;</span><span class="punctuation">,</span><span class="string">&quot;http://hub-mirror.c.163.com&quot;</span><span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h4 id="hint：可以使用nano命令直接在云主机上编辑daemon-json，使用方法请自行查找。也可以在本地用纯文本编辑器（如txt）编辑，然后上传到云主机。"><a href="#hint：可以使用nano命令直接在云主机上编辑daemon-json，使用方法请自行查找。也可以在本地用纯文本编辑器（如txt）编辑，然后上传到云主机。" class="headerlink" title="hint：可以使用nano命令直接在云主机上编辑daemon.json，使用方法请自行查找。也可以在本地用纯文本编辑器（如txt）编辑，然后上传到云主机。"></a>hint：可以使用<code>nano</code>命令直接在云主机上编辑daemon.json，使用方法请自行查找。也可以在本地用纯文本编辑器（如txt）编辑，然后上传到云主机。</h4><h4 id="之后重新启动docker即可"><a href="#之后重新启动docker即可" class="headerlink" title="之后重新启动docker即可"></a>之后重新启动<code>docker</code>即可</h4><h4 id="b-测试Docker是否安装成功"><a href="#b-测试Docker是否安装成功" class="headerlink" title="b) 测试Docker是否安装成功"></a>b) 测试Docker是否安装成功</h4><h4 id="当你的按照上述步骤安装完docker之后，可以通过运行如下命令来测试docker是否安装成功"><a href="#当你的按照上述步骤安装完docker之后，可以通过运行如下命令来测试docker是否安装成功" class="headerlink" title="当你的按照上述步骤安装完docker之后，可以通过运行如下命令来测试docker是否安装成功"></a>当你的按照上述步骤安装完docker之后，可以通过运行如下命令来测试docker是否安装成功</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">$ docker run hello-world</span><br><span class="line"></span><br><span class="line">Hello from Docker.</span><br><span class="line">This message shows that your installation appears to be working correctly.</span><br><span class="line">...</span><br></pre></td></tr></table></figure><h4 id="3-尝试使用busybox"><a href="#3-尝试使用busybox" class="headerlink" title="3. 尝试使用busybox"></a>3. 尝试使用busybox</h4><blockquote><p>到此为止，你的docker环境已经安装完成并且正常运行，接下来我们尝试一些更加复杂的工作</p></blockquote><h4 id="在这个部分，我们将要通过docker-run命令去开启一个BusyBox-container"><a href="#在这个部分，我们将要通过docker-run命令去开启一个BusyBox-container" class="headerlink" title="在这个部分，我们将要通过docker run命令去开启一个BusyBox container"></a>在这个部分，我们将要通过<code>docker run</code>命令去开启一个<code>BusyBox container</code></h4><h4 id="a-首先，我们通过运行如下命令从镜像库拉取BusyBox的镜像"><a href="#a-首先，我们通过运行如下命令从镜像库拉取BusyBox的镜像" class="headerlink" title="a) 首先，我们通过运行如下命令从镜像库拉取BusyBox的镜像:"></a>a) 首先，我们通过运行如下命令从镜像库拉取<code>BusyBox</code>的镜像:</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ docker pull busybox</span><br></pre></td></tr></table></figure><blockquote><p>如果运行过程中遇到  <strong>permission denied</strong> 此类的错误，可以尝试在命令前加入 <strong>sudo</strong> 命令</p></blockquote><h4 id="b-pull-命令可以将busybox-image从docker仓库中拉取到本地，你可以使用docker-image命令查看系统上目前已有的image"><a href="#b-pull-命令可以将busybox-image从docker仓库中拉取到本地，你可以使用docker-image命令查看系统上目前已有的image" class="headerlink" title="b) pull 命令可以将busybox image从docker仓库中拉取到本地，你可以使用docker image命令查看系统上目前已有的image."></a>b) <strong>pull</strong> 命令可以将<code>busybox image</code>从docker仓库中拉取到本地，你可以使用<strong>docker image</strong>命令查看系统上目前已有的<strong>image</strong>.</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">$ docker images</span><br><span class="line"></span><br><span class="line">REPOSITORY TAG IMAGE ID CREATEDVIRT</span><br><span class="line">busybox latest c51f86c28340 4 weeks ago 1.10</span><br></pre></td></tr></table></figure><h4 id="c-我们接下来继续尝试使用docker-run命令来运行busybox"><a href="#c-我们接下来继续尝试使用docker-run命令来运行busybox" class="headerlink" title="c) 我们接下来继续尝试使用docker run命令来运行busybox"></a>c) 我们接下来继续尝试使用<code>docker run</code>命令来运行<code>busybox</code></h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$ docker run busybox</span><br><span class="line">$</span><br></pre></td></tr></table></figure><h4 id="d-可以发现命令运行完，什么也没有发生。这不是bug，当我们执行docker-run命令时，docker会从本地查找image，然后加载image生成container容器，并在其中运行command。正因为我们没有运行任何command，此容器没有任何输出。让我们加入自定义的command再试一次。"><a href="#d-可以发现命令运行完，什么也没有发生。这不是bug，当我们执行docker-run命令时，docker会从本地查找image，然后加载image生成container容器，并在其中运行command。正因为我们没有运行任何command，此容器没有任何输出。让我们加入自定义的command再试一次。" class="headerlink" title="d) 可以发现命令运行完，什么也没有发生。这不是bug，当我们执行docker run命令时，docker会从本地查找image，然后加载image生成container容器，并在其中运行command。正因为我们没有运行任何command，此容器没有任何输出。让我们加入自定义的command再试一次。"></a>d) 可以发现命令运行完，什么也没有发生。这不是bug，当我们执行<code>docker run</code>命令时，docker会从本地查找<code>image</code>，然后加载<code>image</code>生成<code>container</code>容器，并在其中运行command。正因为我们没有运行任何command，此容器没有任何输出。让我们加入自定义的command再试一次。</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$ docker run busybox <span class="built_in">echo</span> <span class="string">&quot;hello from busybox&quot;</span></span><br><span class="line">hello from busybox</span><br></pre></td></tr></table></figure><h5 id="hint：通常-docker-image不在运行时叫image，加载运行以后叫container"><a href="#hint：通常-docker-image不在运行时叫image，加载运行以后叫container" class="headerlink" title="hint：通常,docker image不在运行时叫image，加载运行以后叫container"></a>hint：通常,docker image不在运行时叫image，加载运行以后叫container</h5>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Docker基本命令</title>
    <link href="/2023/04/01/Docker%E5%9F%BA%E6%9C%AC%E5%91%BD%E4%BB%A4/"/>
    <url>/2023/04/01/Docker%E5%9F%BA%E6%9C%AC%E5%91%BD%E4%BB%A4/</url>
    
    <content type="html"><![CDATA[<h3 id="hint：docker镜像库中包含大量可用镜像，可以直接下载到本地使用，常用的镜像库比如https-hub-docker-com"><a href="#hint：docker镜像库中包含大量可用镜像，可以直接下载到本地使用，常用的镜像库比如https-hub-docker-com" class="headerlink" title="hint：docker镜像库中包含大量可用镜像，可以直接下载到本地使用，常用的镜像库比如https://hub.docker.com/"></a>hint：docker镜像库中包含大量可用镜像，可以直接下载到本地使用，常用的镜像库比如<a href="https://hub.docker.com/">https://hub.docker.com/</a></h3><h4 id="1-在https-cloud-docker-com，免费注册一个-Docker-账号。用以下命令登录docker-hub，输入创建账号的用户名、密码"><a href="#1-在https-cloud-docker-com，免费注册一个-Docker-账号。用以下命令登录docker-hub，输入创建账号的用户名、密码" class="headerlink" title="1.在https://cloud.docker.com，免费注册一个 Docker 账号。用以下命令登录docker hub，输入创建账号的用户名、密码"></a>1.在<a href="https://cloud.docker.com,免费注册一个/">https://cloud.docker.com，免费注册一个</a> Docker 账号。用以下命令登录<code>docker hub</code>，输入创建账号的用户名、密码</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">$ docker login </span><br><span class="line">Login with your Docker ID to push and pull images from Docker Hub. If you don<span class="string">&#x27;t have a Docker ID, head over to https://hub.docker.com to create one.</span></span><br><span class="line"><span class="string">Username: linlinshe</span></span><br><span class="line"><span class="string">Password: </span></span><br><span class="line"><span class="string">WARNING! Your password will be stored unencrypted in /root/.docker/config.json.</span></span><br><span class="line"><span class="string">Configure a credential helper to remove this warning. See</span></span><br><span class="line"><span class="string">https://docs.docker.com/engine/reference/commandline/login/#credentials-store</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">Login Succeeded</span></span><br></pre></td></tr></table></figure><h4 id="出现login-succeeded表示登陆成功"><a href="#出现login-succeeded表示登陆成功" class="headerlink" title="出现login succeeded表示登陆成功"></a>出现<code>login succeeded</code>表示登陆成功</h4><h4 id="2-将如下内容写入-etc-docker-daemon-json中（没有此文件则需要新建-ps-需保证docker已经运行过，否则会出现-etc-docker文件夹不存在）"><a href="#2-将如下内容写入-etc-docker-daemon-json中（没有此文件则需要新建-ps-需保证docker已经运行过，否则会出现-etc-docker文件夹不存在）" class="headerlink" title="2.将如下内容写入/etc/docker/daemon.json中（没有此文件则需要新建. ps: 需保证docker已经运行过，否则会出现/etc/docker文件夹不存在）"></a>2.将如下内容写入<code>/etc/docker/daemon.json</code>中（没有此文件则需要新建. ps: 需保证<code>docker</code>已经运行过，否则会出现<code>/etc/docker</code>文件夹不存在）</h4><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;registry-mirrors&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;https://uhub-edu.service.ucloud.cn&quot;</span><span class="punctuation">,</span><span class="string">&quot;https://uhub.service.ucloud.cn&quot;</span><span class="punctuation">,</span><span class="string">&quot;https://registry.docker-cn.com&quot;</span><span class="punctuation">,</span><span class="string">&quot;http://hub-mirror.c.163.com&quot;</span><span class="punctuation">]</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h4 id="hint：可以使用nano命令直接在云主机上编辑daemon-json，使用方法请自行查找。也可以在本地用纯文本编辑器（如txt）编辑，然后上传到云主机。"><a href="#hint：可以使用nano命令直接在云主机上编辑daemon-json，使用方法请自行查找。也可以在本地用纯文本编辑器（如txt）编辑，然后上传到云主机。" class="headerlink" title="hint：可以使用nano命令直接在云主机上编辑daemon.json，使用方法请自行查找。也可以在本地用纯文本编辑器（如txt）编辑，然后上传到云主机。"></a>hint：可以使用<code>nano</code>命令直接在云主机上编辑daemon.json，使用方法请自行查找。也可以在本地用纯文本编辑器（如txt）编辑，然后上传到云主机。</h4><h4 id="之后重新启动docker即可"><a href="#之后重新启动docker即可" class="headerlink" title="之后重新启动docker即可"></a>之后重新启动<code>docker</code>即可</h4><h4 id="b-测试Docker是否安装成功"><a href="#b-测试Docker是否安装成功" class="headerlink" title="b) 测试Docker是否安装成功"></a>b) 测试Docker是否安装成功</h4><h4 id="当你的按照上述步骤安装完docker之后，可以通过运行如下命令来测试docker是否安装成功"><a href="#当你的按照上述步骤安装完docker之后，可以通过运行如下命令来测试docker是否安装成功" class="headerlink" title="当你的按照上述步骤安装完docker之后，可以通过运行如下命令来测试docker是否安装成功"></a>当你的按照上述步骤安装完docker之后，可以通过运行如下命令来测试docker是否安装成功</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">$ docker run hello-world</span><br><span class="line"></span><br><span class="line">Hello from Docker.</span><br><span class="line">This message shows that your installation appears to be working correctly.</span><br><span class="line">...</span><br></pre></td></tr></table></figure><h4 id="3-尝试使用busybox"><a href="#3-尝试使用busybox" class="headerlink" title="3. 尝试使用busybox"></a>3. 尝试使用busybox</h4><blockquote><p>到此为止，你的docker环境已经安装完成并且正常运行，接下来我们尝试一些更加复杂的工作</p></blockquote><h4 id="在这个部分，我们将要通过docker-run命令去开启一个BusyBox-container"><a href="#在这个部分，我们将要通过docker-run命令去开启一个BusyBox-container" class="headerlink" title="在这个部分，我们将要通过docker run命令去开启一个BusyBox container"></a>在这个部分，我们将要通过<code>docker run</code>命令去开启一个<code>BusyBox container</code></h4><h4 id="a-首先，我们通过运行如下命令从镜像库拉取BusyBox的镜像"><a href="#a-首先，我们通过运行如下命令从镜像库拉取BusyBox的镜像" class="headerlink" title="a) 首先，我们通过运行如下命令从镜像库拉取BusyBox的镜像:"></a>a) 首先，我们通过运行如下命令从镜像库拉取<code>BusyBox</code>的镜像:</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ docker pull busybox</span><br></pre></td></tr></table></figure><blockquote><p>如果运行过程中遇到  <strong>permission denied</strong> 此类的错误，可以尝试在命令前加入 <strong>sudo</strong> 命令</p></blockquote><h4 id="b-pull-命令可以将busybox-image从docker仓库中拉取到本地，你可以使用docker-image命令查看系统上目前已有的image"><a href="#b-pull-命令可以将busybox-image从docker仓库中拉取到本地，你可以使用docker-image命令查看系统上目前已有的image" class="headerlink" title="b) pull 命令可以将busybox image从docker仓库中拉取到本地，你可以使用docker image命令查看系统上目前已有的image."></a>b) <strong>pull</strong> 命令可以将<code>busybox image</code>从docker仓库中拉取到本地，你可以使用<strong>docker image</strong>命令查看系统上目前已有的<strong>image</strong>.</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">$ docker images</span><br><span class="line"></span><br><span class="line">REPOSITORY TAG IMAGE ID CREATEDVIRT</span><br><span class="line">busybox latest c51f86c28340 4 weeks ago 1.10</span><br></pre></td></tr></table></figure><h4 id="c-我们接下来继续尝试使用docker-run命令来运行busybox"><a href="#c-我们接下来继续尝试使用docker-run命令来运行busybox" class="headerlink" title="c) 我们接下来继续尝试使用docker run命令来运行busybox"></a>c) 我们接下来继续尝试使用<code>docker run</code>命令来运行<code>busybox</code></h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$ docker run busybox</span><br><span class="line">$</span><br></pre></td></tr></table></figure><h4 id="d-可以发现命令运行完，什么也没有发生。这不是bug，当我们执行docker-run命令时，docker会从本地查找image，然后加载image生成container容器，并在其中运行command。正因为我们没有运行任何command，此容器没有任何输出。让我们加入自定义的command再试一次。"><a href="#d-可以发现命令运行完，什么也没有发生。这不是bug，当我们执行docker-run命令时，docker会从本地查找image，然后加载image生成container容器，并在其中运行command。正因为我们没有运行任何command，此容器没有任何输出。让我们加入自定义的command再试一次。" class="headerlink" title="d) 可以发现命令运行完，什么也没有发生。这不是bug，当我们执行docker run命令时，docker会从本地查找image，然后加载image生成container容器，并在其中运行command。正因为我们没有运行任何command，此容器没有任何输出。让我们加入自定义的command再试一次。"></a>d) 可以发现命令运行完，什么也没有发生。这不是bug，当我们执行<code>docker run</code>命令时，docker会从本地查找<code>image</code>，然后加载<code>image</code>生成<code>container</code>容器，并在其中运行command。正因为我们没有运行任何command，此容器没有任何输出。让我们加入自定义的command再试一次。</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$ docker run busybox <span class="built_in">echo</span> <span class="string">&quot;hello from busybox&quot;</span></span><br><span class="line">hello from busybox</span><br></pre></td></tr></table></figure><h5 id="hint：通常-docker-image不在运行时叫image，加载运行以后叫container"><a href="#hint：通常-docker-image不在运行时叫image，加载运行以后叫container" class="headerlink" title="hint：通常,docker image不在运行时叫image，加载运行以后叫container"></a>hint：通常,docker image不在运行时叫image，加载运行以后叫container</h5>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2023/04/01/hello-world/"/>
    <url>/2023/04/01/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
